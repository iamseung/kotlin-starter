package lec01

import com.lannstark.lec01.Person

fun main() {
    var number1 = 10L
    val number2 = 10L

    var number3: Long? = 1_000L // ? 은 Null이 들어갈 수 있음을 표기
    var person = Person("최태현") // 코틀린에선 객체 인스턴스화를 할 때에는 new 를 붙이지 않는다
}

// a
// b1
// b2
// c